---
layout: default
title: Products
permalink: /products.html
---

<section class="top">
	<h1>Grooming products for every types of hairs</h2>
	<p>
		Except balds…
	</p>
</section>
<section class="item-grid">
  {% for entry in products.items %}
	<a href="{{url}}" class="item">
		<img src="{{data.image}}" alt="{{data.name}}" />
		<h2>{{data.name}}</h2>
		<p>
			<button class="snipcart-add-item"
					data-item-id="11ty-{{fileSlug}}"
					data-item-name="{{data.name}}"
					data-item-price="{{data.price}}"
					data-item-image="{{data.image}}"
					data-item-url="{{config.siteUrl}}/products.json">
				Buy for {{data.price}}$
			</button>
		</p>
	</a>
  {% endfor %}
</section>
